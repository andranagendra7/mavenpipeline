#!/usr/bin/env groovy

pipeline {

     agent any
     
     stages {
         
           stage('check') {
               steps {
                        echo "Check docker service status"
                        sh 'docker --version'
                        sh 'docker build -t httpdimage2 .'
                      }
                  }
           
           stage('Prodection') {
               steps {
                        echo "Check docker service status"
                        sh 'systemctl status docker.service'
                      }
                  }
          
           stage('containercreation') {
               steps {
                        echo "Check docker service status"
                        sh 'docker run -dti --name jenkinscon2 -p 88:80  httpdimage2 /bin/bash'
                        //echo "Login into the container and start httpd service"
                        //sh 'docker attach jenkinscon1'
                        //sh 'service httpd start'
                      }
                  }
             }
}
