#!/usr/bin/env groovy

pipeline {

     agent any
     
     stages {
         
           stage('Prodection') {
               steps {
                        echo "Check docker service status"
                        sh 'docker --version'
                      }
                  }
           
           stage('Prodection') {
               steps {
                        echo "Check docker service status"
                        sh 'systemctl status docker.service'
                      }
                  }
           
             }
}
